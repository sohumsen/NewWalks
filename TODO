add splash screen
make map view around points properly
watchfor user is not accurate
keep track of distance travelled
get more api keys
set up app id
set up permissions

future
========
utilise vehicle metrics possibly
Maybe prevent route repititions
search past maps
